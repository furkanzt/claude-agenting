{
  "description": "Two-stage routing gate on the Workflow tool. Stage 1 denies a Workflow whose agent() calls carry no model/agentType or no effort — unrouted calls silently inherit the main session's tier. Stage 2 denies until the user has approved this particular routing plan in this session; approval is keyed by plan signature, so re-running the same plan is silent and changing any tier asks again.",
  "hooks": {
    "PreToolUse": [
      {
        "matcher": "Workflow",
        "hooks": [
          {
            "type": "command",
            "command": "python3 \"${CLAUDE_PLUGIN_ROOT}/hooks/workflow-routing-guard.py\"",
            "timeout": 10,
            "statusMessage": "Checking workflow agent routing..."
          }
        ]
      }
    ]
  }
}
